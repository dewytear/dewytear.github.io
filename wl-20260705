<h2>2026-07-05 · AI 지식 관리 시스템 구축</h2>
<p>"AI가 위키를 라이브로 임베딩해 지식을 참고·정리·집계하고 지속 관리한다"는 목표로, <strong>지식 인덱스 → 시맨틱 검색 → 자동 큐레이터</strong>의 3단계 시스템을 만들었습니다. 마지막에 전체를 3회 반복 검토(감사 → 라이브화 → 종단 검증)로 다듬었습니다.</p>

<h3>1단계 · 지식 인덱스</h3>
<ul>
    <li><strong>knowledge-index.json</strong> — 강의 문서 85편을 AI가 전부 읽어 문서별 <em>요약·핵심 개념·연관 문서</em>를 기록. 연관도는 희소성 가중(idf) 개념 중복 + 같은 폴더 보완. <span class="scn-sub">(PR #91)</span></li>
    <li><strong>🧭 AI 지식 지도</strong> — 8개 클러스터·허브 문서·브리지 개념을 집계한 최상위 문서(번호 없음). 각 문서 하단에 <strong>연관 문서</strong> 카드 노출. <span class="scn-sub">(PR #91·#92)</span></li>
</ul>

<h3>2단계 · 개념 기반 시맨틱 검색</h3>
<ul>
    <li>통합 검색이 <strong>필드 가중 랭킹</strong>(개념 &gt; 제목 &gt; 태그 &gt; 요약 &gt; 본문)으로 의미 매칭 문서를 상단에. 결과 카드에 <code>#개념</code> 칩, 관련 개념 피벗 제안. <span class="scn-sub">(PR #92)</span></li>
    <li><strong>연관 문서 표시 규칙</strong>을 최근 문서와 통일하고 설정에 숨김 토글 추가. <span class="scn-sub">(PR #92)</span></li>
</ul>

<h3>3단계 · 지속 관리(큐레이터)</h3>
<ul>
    <li><strong>재현 가능한 빌드</strong> — <code>tools/doc-entries.json</code>(원료) + <code>tools/build_index.py</code>(결정적 빌드) + <code>tools/curator.md</code>(절차서: 변경 감지 → 엔트리 갱신 → 재빌드 → Work Log 기록 → 검토용 PR, 자동 머지 금지). <span class="scn-sub">(PR #93·#94)</span></li>
</ul>

<h3>3회 반복 검토</h3>
<ul>
    <li><strong>반복 1 · 감사</strong> — 인덱스 85/85 완전성·related 무결성 통과. 지도의 수동 수치가 인덱스와 어긋난 <strong>드리프트 발견</strong>.</li>
    <li><strong>반복 2 · 라이브화</strong> — 지도 집계(클러스터·허브·브리지·핵심 개념·총계)를 인덱스의 <code>stats</code>에서 <strong>실시간 렌더</strong>로 전환해 드리프트 원천 차단. 지도·연관 카드의 <strong>개념 칩 클릭 → 통합 검색</strong> 피벗. <span class="scn-sub">(PR #95)</span></li>
    <li><strong>반복 3 · 종단 검증</strong> — 격리 사본에서 가상 문서 추가·수정으로 큐레이터 전 과정(감지→갱신→재빌드→집계 반영)을 실증. 이 과정에서 <strong>부동소수 합산 순서로 인한 비결정성 버그를 발견·수정</strong>(5개 해시시드 재현 확인). <span class="scn-sub">(PR #96)</span></li>
</ul>

<h3>오후 · UI와 미니게임</h3>
<ul>
    <li><strong>연관 문서 버그 수정</strong> — 메뉴로 문서를 이동해도 이전 문서의 연관 문서가 남던 문제(setArticle 잔여물 정리 누락). <span class="scn-sub">(PR #97)</span></li>
    <li><strong>통합검색 미니게임 4종</strong> — 벽돌깨기(패들 드래그·반사각 조절), 개념 별똥별 받기(받으면 그 개념으로 검색), 퐁 랠리(공속 2배), 종이비행기 글라이딩. 설정은 기본값, <strong>방문자는 화면 하단 게임 독에서 직접 선택</strong>(브라우저별 기억). <span class="scn-sub">(PR #98~#102)</span></li>
    <li><strong>Work Log 표시 규칙</strong> — 태그처럼 최근 문서 영역도 Work Log에선 숨김. <span class="scn-sub">(PR #100)</span></li>
    <li><strong>클로드식 레이아웃</strong> — 틴트 셸이 둥근 본문 카드를 감싸는 claude.ai풍 디자인, 좌측 목록은 부드러운 슬라이드 개폐(가로모드 기본 = 가림, 선택 기억). <span class="scn-sub">(PR #103·#104)</span></li>
    <li><strong>&#10022; 지식 코스모스</strong> — 지식 인덱스의 연관 관계를 3차원 별자리로: 문서=별(피연결수록 크게), 연관=연결선, 드래그 회전·휠 확대·별 클릭 시 문서 이동. <span class="scn-sub">(PR #104)</span></li>
</ul>

<h3>저녁 · 코스모스 다듬기와 디자인 정밀화</h3>
<ul>
    <li><strong>코스모스 진화</strong> — 화면을 가득 채우게 확대, 네비 <strong>최상위 맨 위로 승격</strong>(지식 지도는 AI 아래 유지), 분류가 다른 문서는 <strong>서로 다른 은하</strong>(이름표 달린 성단)로 분리 — 분류를 넘는 연관 링크가 은하 간 다리로 보임. <span class="scn-sub">(PR #106·#111·#113)</span></li>
    <li><strong>클로드식 셸 완성</strong> — 본문 카드 테두리 제거(메뉴 옆 세로선 소멸), Tags·설정 푸터 배경을 셸과 통일, 타이틀-본문 경계선 제거, 남아 있던 사이드바 구분선까지 정리. 카드가 배경 대비와 그림자만으로 떠 있는 감싸는 구도. <span class="scn-sub">(PR #107~#109·#111)</span></li>
    <li><strong>카드 상태별 모핑</strong> — 메뉴 접힘(전체 화면)에선 카드 단서가 사라져 풀 페이지처럼, 메뉴가 열리면 은은한 카드로 — claude.ai와 같은 동작. <span class="scn-sub">(PR #115)</span></li>
    <li><strong>통합검색 배경 확장</strong> — 호흡 배경과 미니게임이 타이틀 뒤까지 카드 전체를 무대로 사용(게임 좌표계 정합 포함). 검색창 포커스는 은은한 테두리 틴트만 남기고 차분하게. <span class="scn-sub">(PR #110·#112)</span></li>
    <li><strong>아이콘 모노크롬 통일</strong> — 컬러 이모지를 테마 색을 따르는 글리프로: 코스모스 &#10022;, 지식 지도 &#10070;, 폴더 모아보기 &#10697;. <span class="scn-sub">(PR #113·#114)</span></li>
</ul>

<div class="note">
    <span class="nh">남은 것</span>
    매일 자동 실행 예약(트리거)은 앱 권한 승인이 필요해 대기 중. 승인 전까지는 요청 시 수동으로 큐레이터를 실행한다(절차·도구는 검증 완료).
</div>
