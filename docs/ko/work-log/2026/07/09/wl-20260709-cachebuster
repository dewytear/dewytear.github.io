<h2>캐시버스터 갱신 — 새 글 표시 기능이 재방문 브라우저에 반영되게</h2>
<p><a href="#!wl-20260709-newdoc-markers">새 글 표시 기능</a>을 머지했으나 "반영이 안 된다"는 보고. 원인은 <strong>캐시버스터 누락</strong>이었다: <code>app.js</code>·<code>search.js</code>·<code>style.css</code>·<code>i18n.js</code> 내용은 바꿨지만 <code>index.html</code>의 <code>?v=</code> 쿼리를 올리지 않아, 재방문 브라우저(특히 iOS Safari)가 <code>max-age</code> 캐시의 옛 자산을 계속 썼다.</p>

<h3>수정</h3>
<ul>
    <li><code>index.html</code>의 <code>?v=</code> 상향: <code>app.js</code> 3→4, <code>search.js</code> 1→2, <code>style.css</code> 13→14, <code>i18n.js</code> 2→3.</li>
    <li>바뀐 파일만 올렸다(변경 없는 <code>colors/core/utils/cosmos/games/graphviews/folder-dates/music</code>는 그대로).</li>
</ul>

<div class="note warn">
    <span class="nh">교훈</span>
    CLAUDE.md 배포 규칙: <code>app.js</code>·<code>style.css</code> 등 캐시되는 자산을 고치면 <strong>같은 PR에서 <code>?v=</code>를 반드시 올린다</strong>. 이번엔 기능 PR에서 빠뜨려 별도 보완 머지가 필요했다 — 다음부터 자산 변경과 버전 상향을 한 커밋에 묶는다.
</div>
