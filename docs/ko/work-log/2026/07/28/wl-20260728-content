<h2>2026-07-28 · 콘텐츠 — 일본어 배치 10 (남은 뉴스 기사 5편)</h2>

<p>열 번째 배치. News &amp; Articles 구간에서 가장 무거운 다섯 편 — <code>thariq-fable5-unknowns</code>(50KB) · <code>llm-wiki-knowledge-context</code>(30KB) · <code>oracle-ontology-property-graph</code>(35KB) · <code>kimi-k3</code>(38KB) · <code>claude-opus-5</code>(21KB), 합 <strong>175KB · 도식 12개 · 래스터 이미지 4장 · 유튜브 임베드 1개</strong> — 을 일본어로 옮겼다. 이로써 ja 본문은 <strong>104 / 115편(90%)</strong>. 변경 유형: <strong>CONTENT</strong>.</p>
<p>작업은 <a href="#!wl-20260727-content">어제 배치 9</a>에서 이어지지만, <strong>날짜가 KST 자정을 넘겨 그날 로그로 분리</strong>했다(어제 세운 날짜 규칙 그대로 — 같은 주제라도 날짜가 우선).</p>

<h3>번역 방침 — 사실의 뼈대는 손대지 않는다</h3>
<ul>
    <li><strong>수치·날짜·URL·벤치마크 이름 전부 원본 유지</strong> — Kimi K3의 2.8T/~50B·57.1점·1,679 Elo·76% 승률·283.6ms→114.4ms, Opus 5의 $5/$25·43.3% vs 34.4%·0.5% 이내, 타릭 기사의 &ldquo;약 80%&rdquo;·280만 조회. 특히 두 기사는 <strong>&ldquo;확인된 사실 / 발표상 주장 / 개인 일화&rdquo;를 나눠 적는 것 자체가 본문의 요지</strong>라, 판정 열의 어휘(<code>公式に確認</code>·<code>タリクの公開の発表上の主張</code>·<code>個人の逸話</code>·<code>時点に依存</code>)를 일관되게 고정했다.</li>
    <li><strong>영어 개념어는 번역하지 않는다</strong> — capability overhang · unhobbling · unknown unknowns · map/territory · deviation · buy-in · semantics map · provenance. 타릭 기사는 이 어휘 자체가 프레임이라, 일본어 조사만 붙여 그대로 뒀다(<code>unknown を known へ昇格</code>).</li>
    <li><strong>프롬프트 예시는 &ldquo;의미 보존 재구성&rdquo;임을 명시</strong> — 원문 주석 &ldquo;원문의 의미를 보존한 한국어 재구성&rdquo;을 <code>原文の意味を保った日本語の再構成</code>으로 옮겨, 일본어 독자도 <strong>이것이 타릭의 원문 그대로가 아님</strong>을 알 수 있게 했다.</li>
    <li><strong>용어 정본</strong>: 하네스→<code>ハーネス</code> · 오픈웨이트→<code>オープンウェイト</code> · 전문가 혼합→<code>専門家の混合(MoE)</code> · 잠재추론→<code>潜在での推論</code> · 날리지 컨텍스트→<code>ナレッジコンテキスト</code> · 승격 플로우→<code>昇格のフロー</code> · 계층 로딩→<code>階層の読み込み</code> · 조직 원리→<code>組織の原理</code> · 엣지 테이블(H)→<code>エッジ表(H 表)</code> · 홉→<code>ホップ</code> · 사각지대→<code>死角</code>.</li>
</ul>

<div class="note">
    <span class="nh">일본어라 좁아진 상자 — 이번엔 &ldquo;줄이기&rdquo;로 풀었다</span>
    배치 9에서는 상자를 넓혔지만, 이번 도식들은 <strong>격자로 배열된 작은 상자</strong>(92px·90px)라 하나를 넓히면 줄 전체가 어긋난다. 그래서 <strong>부제(d-sub)를 줄이는 쪽</strong>으로 맞췄다 — 타릭 기사 11패턴 격자에서 <code>질문 못한 제약</code>→<code>聞けない制約</code> · <code>판단 어휘 학습</code>→<code>判断の語彙</code> · <code>개입 공간 확장</code>→<code>介入点を広げる</code> · <code>이탈·가정 기록</code>→<code>逸脱・仮定</code>. 상단의 패턴 이름(<code>1 · Blindspot</code> 등)은 원문 그대로라 정보 손실이 없다.<br>
    LLM Wiki 기사의 RAG 파이프라인도 110px 상자 5개가 붙어 있어 <code>청크·임베딩</code>을 <code>チャンク埋込</code>로, <code>검색 없이 문서째</code>를 <code>検索なしで丸ごと</code>로 줄였다. 오라클 기사의 와인 테이블 행은 전각 구분점(<code>・</code>)으로 바꾸고 <code>ヴィンテージ</code>(6자)를 <code>年式</code>(2자)로 줄여 340px 안에 넣었다.
</div>

<p><code>check_diagram_bounds</code> <strong>228개 전수 통과</strong>(ja 5편 신규). <code>validate_all</code> ERROR 0 · WARN 23 유지. 브라우저에서 5편 모두 일본어 h2 · pageerror 0 · ko/en 회귀 무변화이고, 래스터 이미지 4장이 실제로 로드되는 것(<code>naturalWidth</code> 1440·1205·1440·1440), Kimi 기사의 <strong>cmp 막대 23개</strong>와 Opus 5 기사의 <strong>유튜브 임베드 1개</strong>가 살아 있는 것을 따로 확인했다.</p>

<p><strong>다음</strong>: 배치 11 — DOUZONE 12편(<code>dz-map</code> · <code>dzp-*</code> 8편 · <code>sne-*</code> 3편). 그다음이 마감 PR(<code>ai-guide</code>·<code>ai-map</code> + <code>check_translation.py</code>의 ja 강제 + 규칙 문서 갱신).</p>

<h3>일본어(ja) 배치 11 — DOUZONE 12편 + 폴더 제목 일본어화</h3>
<p>열한 번째 배치이자 <strong>본문 번역의 마지막 콘텐츠 배치</strong>. DOUZONE World 전체 — 지식지도 <code>dz-map</code>, AI 개발 지식 플랫폼 제안 8편, AI 알림 발송엔진 요청 3편(합 98KB · 도식 9개) — 을 옮겼다. 이로써 ja 본문은 <strong>113 / 115편(98%)</strong>이고, 남은 둘은 마감 PR이 담당할 <code>ai-guide</code>·<code>ai-map</code>뿐이다.</p>
<ul>
    <li><strong>사내 제안서라 어휘를 먼저 고정했다</strong> — 지식 플랫폼→<code>ナレッジプラットフォーム</code> · 지식 상태→<code>Knowledge State</code>(원어 유지) · 가치 판정→<code>価値の判定</code> · 무결성 게이트→<code>無矛盾のゲート</code> · 증분 분석→<code>増分の分析</code> · 신뢰 등급→<code>信頼の水準</code> · 수명주기→<code>寿命の管理</code> · 선순환→<code>好循環</code> · 발송요청→<code>送信要求</code> · 멱등→<code>冪等</code> · 전이형→<code>遷移型</code>.</li>
    <li><strong>제도·조직 고유명사는 원문 유지</strong> — OmniEsol · SCM구축개발1Unit · GitLab · Kafka · DLQ · FACT/INFERRED/DECISION · PUBLIC~RESTRICTED. 스키마 예시의 필드명(<code>identifiers</code>·<code>sources</code>·<code>trust</code>)과 값(<code>SD_SO_HDR</code> 등)도 한 글자도 바꾸지 않았다 — 바꾸면 예시가 예시가 아니게 된다.</li>
    <li><strong>주석은 번역하고 코드는 두었다</strong> — JSON 예시의 <code>// 불변 논리 ID = 라우트</code> 같은 설명 주석은 일본어로, 키·값은 그대로.</li>
</ul>
<div class="note">
    <span class="nh">라벨 하나를 고쳐 본문과 맞췄다</span>
    PR 1에서 넣어 둔 ja 클러스터 라벨이 <code>AI通知配信エンジン</code>이었는데, 본문 번역에서 발송엔진을 <code>送信エンジン</code>으로 확정했다. 사이드바 라벨과 본문 용어가 어긋나면 같은 것을 가리키는지 알기 어려우므로 <code>tools/clusters.json</code>의 ja 라벨을 <code>AI通知の送信エンジン</code>으로 맞췄고, <code>dz-map</code>의 <code>data-topics</code>(ko 문서가 전 언어 검색 키를 함께 담는 구조)에도 ja 키 두 개를 추가했다 — 안 넣으면 <code>validate_i18n</code>이 WARN을 띄운다.
</div>
<div class="note">
    <span class="nh">폴더 제목이 일본어 화면에서 한글로 남아 있었다</span>
    브라우저 검증에서 ja 페이지의 <strong>빵부스러기(breadcrumb)에 &ldquo;AI 개발 지식 플랫폼&rdquo;</strong>이 한글로 떠 있는 것을 발견했다. 문서 라벨(<code>label_ja</code>)은 배치마다 달았지만 <strong>폴더 노드의 <code>title_ja</code>는 한 번도 달지 않아</strong> ko로 폴백하고 있었던 것 — <code>title_en</code>은 28개가 이미 있으니 영어 독자만 제대로 보던 셈이다. 이번 PR에서 <strong>29개 전부</strong>(지식 체계·News &amp; Articles·제안 2종·07월·날짜 24개)에 <code>title_ja</code>를 넣어 닫았다. 재검증에서 breadcrumb 한글은 사라졌다.
</div>
<p><code>check_diagram_bounds</code> <strong>237개 전수 통과</strong>(ja 9편 신규). <code>validate_all</code> ERROR 0 · WARN 23 유지. 브라우저에서 12편 모두 일본어 h2 · pageerror 0 · ko/en 회귀 무변화.</p>
<p class="scn-sub">본문에 남는 한글은 이제 <strong>개념 칩뿐</strong>이다(지식플랫폼·조직지식·큐레이터…). 개념 표시명 사전(<code>concepts.ja.json</code>)이 아직 없어 ko 키가 그대로 보이는 것이라 이번 배치가 만든 퇴행은 아니며, 사전 생성은 별도 주제로 다룰 일이다.</p>
<p><strong>다음</strong>: 마감 PR — <code>ai-guide</code>·<code>ai-map</code> 번역, <code>check_translation.py</code>에 ja 강제 추가(사용자 결정 &ldquo;영어처럼 강제&rdquo;), <code>tools/i18n.md</code>·<code>CLAUDE.md</code>·README 갱신, 설정 드롭다운의 &ldquo;번역 진행 중&rdquo; 표기 제거.</p>

<h3>일본어(ja) 마감 — 지도 2편 + 게이트 강제 + 규칙 문서</h3>
<p>마지막. 남아 있던 <code>ai-map</code>·<code>ai-guide</code>를 옮겨 <strong>인덱스 문서 115편이 전부 일본어로 병행</strong>됐고(<code>docs/ja/</code> 미충족 0건), 이제 규칙을 <strong>기계가 강제</strong>하도록 게이트를 켰다. 대표님 결정 &ldquo;<strong>영어처럼 강제</strong>&rdquo;를 그대로 구현한 것이다. 변경 유형: <strong>CONTENT + TOOL + ENGINE</strong>.</p>
<ul>
    <li><strong>본문 2편</strong> — <code>ai-map</code>(지식지도, 도식 2개 · <code>data-topics</code> 키를 ja 클러스터 라벨로) · <code>ai-guide</code>(AI 순회 계약). ai-guide의 스냅샷 경로 설명에는 <code>/p/ja/&lt;name&gt;/</code>를 추가했다 — 일본어 스냅샷이 실재하는데 계약서에만 없으면 기계가 못 찾는다.</li>
    <li><strong><code>check_translation.py</code>를 언어 목록 기반으로 일반화</strong> — <code>en</code> 하드코딩을 <code>ENFORCED_LANGS = ('en', 'ja')</code> 순회로 바꿨다. 다음 언어는 <strong>소급 번역을 끝낸 뒤</strong> 이 튜플에 한 줄 넣으면 되고, 끝나기 전에 켜면 모든 PR이 막힌다는 경고를 독스트링에 박았다.</li>
    <li><strong>설정의 &ldquo;번역 진행 중&rdquo; 제거</strong> — <code>langJa</code>가 세 언어 사전에서 각각 <code>日本語 — 준비 중</code>/<code>— in progress</code>/<code>— 翻訳中</code>이었다. 이제 그냥 <code>日本語</code>(en 사전만 <code>日本語 (Japanese)</code>)이고, <code>zh</code>는 여전히 &ldquo;준비 중&rdquo;이다. <code>i18n.js?v=21→22</code>.</li>
    <li><strong>규칙 문서 3종</strong> — <code>tools/i18n.md</code>(정책 절 제목을 &ldquo;전면 병행 정책 — en·ja&rdquo;로, 도식 게이트가 전 언어를 순회한다는 사실과 격자 도식에서는 넓히는 대신 부제를 줄인다는 실무 지침 추가, 현황 절에 ja 완료 기록), <code>CLAUDE.md</code> 다국어 규칙(en·ja 동반 + 일본어는 가나가 전각이라 글자 수가 늘기 쉽다는 주의), README(&ldquo;Korean and English&rdquo; → 3언어).</li>
    <li><strong>Backlog</strong>의 &ldquo;최근 완료&rdquo;에 일본어 전면 병행을 올렸다.</li>
</ul>
<div class="note">
    <span class="nh">브라우저에서 확인한 것</span>
    ja·ko·en 각각에서 지도 2편의 h2가 그 언어이고 <strong>pageerror 0</strong>, 클러스터 표의 하이드레이션 첫 칸이 <code>知識体系</code>/<code>지식 체계</code>/<code>Knowledge System</code>으로 각 언어의 라벨을 집어 온다. 사이드바 RSS 링크도 <code>feed.ja.xml</code>/<code>feed.xml</code>/<code>feed.en.xml</code>로 갈린다. 설정 화면은 비밀번호 뒤라 셀렉트를 직접 못 여니 <strong>페이지 안에서 <code>STR('langJa')</code>를 호출해</strong> 세 사전 모두 &ldquo;진행 중&rdquo; 표기가 사라진 것을 확인했다.
</div>
<p><code>check_diagram_bounds</code> <strong>238개 전수 통과</strong> · <code>validate_all</code> ERROR 0 · WARN 23 유지 · <code>check_translation</code>이 <code>(en, ja)</code>로 통과.</p>
<div class="note warn">
    <span class="nh">솔직한 전제 — 이걸로 방문자가 늘지는 않는다</span>
    영어 전면 병행이 유입을 늘리지 못했고, 일본어도 <strong>그 자체로는 사람을 데려오지 않는다</strong>. 검색 등록·커뮤니티 노출이 선행돼야 의미가 생기며, 그 항목들은 여전히 <a href="#!wl-backlog">Backlog</a>의 &ldquo;지금 할 일&rdquo;에 사람 몫으로 남아 있다. 이 작업의 값은 <strong>그 유입이 생겼을 때 일본어 독자가 폴백된 한국어를 만나지 않는다</strong>는 데 있다.
</div>
<p class="scn-sub"><strong>남은 것</strong>: 개념 표시명 사전(<code>concepts.ja.json</code> 미생성 — 개념 칩이 ko 키 그대로). 영어판 <code>concepts.en.json</code>은 302건이 갖춰져 있으니 <strong>일본어만 비어 있는 상태</strong>이며, 별도 주제로 다룬다.</p>

<h3>아티클 — 하네스로는 부족하다(소프트웨어 공장이 실패하는 이유)</h3>
<p>대표님이 강연 전문과 ChatGPT가 만든 HTML 재구성본을 주시며 <strong>원본 강연을 주제로 아티클을 써 달라</strong>고 하셨다. 웹 검색으로 사실을 교차검증하고 3개 언어로 등재했다.</p>

<div class="note">
    <span class="nh">무엇을 검증했나</span>
    발표자·행사를 확정했다 &mdash; <strong>Dex Horthy(HumanLayer)</strong>, <strong>AI Engineer World's Fair 2026</strong>, 「Harness Engineering is not Enough: Why Software Factories Fail」. 전문에는 없던 수치도 원출처에서 채웠다: Faros AI <em>Acceleration Whiplash</em>(2026-03)의 <strong>개발자 22,000명·4,000여 팀·2년치</strong> 표본과 버그 +54%·인시던트 3배·리뷰 시간 5배·무리뷰 머지 +31.3%, lights-out 실험 기간이 <strong>2025년 7~11월</strong>이라는 것, Cognition <strong>FrontierCode</strong>의 채점 항목 5종·위양성 81% 감소·Diamond 최난도에서 최고 모델 <strong>약 13%</strong>.
</div>

<p>글의 축은 <strong>&ldquo;왜 하네스로는 안 되는가&rdquo;</strong> 하나로 잡았다. 강연의 논증이 <em>RL 채점표에 설계 품질 칸이 없다 → 그래서 테스트만 통과시키는 행동이 강화된다 → 유지보수성은 보상이 몇 달 뒤에 와서 신호로 못 쓴다</em>로 이어지는데, 이 사슬을 이해하면 나머지(리뷰를 없애면 안 되는 이유, 앞단 30분 정렬)가 따라온다. 가장 날카로운 한 줄 &mdash; <strong>&ldquo;모델이 좋은 코드를 알았다면 애초에 그렇게 썼을 것&rdquo;</strong> &mdash; 을 별도 상자로 세웠다.</p>

<p><strong>슬라이드는 그대로 쓰지 않고 인라인 SVG 도식 2개로 다시 그렸다</strong>(사용자가 &ldquo;가능하면 원문 슬라이드 채용&rdquo;을 허용했으나): ①외부 이미지 핫링크는 저장소 규칙상 금지이고 ②샌드박스에서 슬라이드 이미지를 받을 수 없었으며 ③무엇보다 도식으로 그리면 <strong>테마 전환·3개 언어 번역·폭 게이트</strong>가 전부 적용된다. RL 보상 루프와 4단계 정렬을 각각 한 장으로 압축했다.</p>

<div class="note warn">
    <span class="nh">번역에서 두 번 샜다 &mdash; 기계가 아니라 눈으로 잡았다</span>
    일본어 본문에 <strong>러시아어 단어(<code>указывать</code>)</strong>와 <strong>영어 단어(<code>approach</code>)</strong>가 섞여 들어갔다. 둘 다 문법적으로는 문장이 성립해 어떤 게이트에도 걸리지 않는다 &mdash; <code>check_i18n_render</code>는 <strong>한글</strong>만 찾고, 도식 게이트는 폭만 본다. 작성 직후 라틴·키릴 문자를 정규식으로 훑어 발견했다. <strong>번역 품질은 여전히 사람(과 스스로의 점검)의 몫</strong>이라는 표시다.
</div>

<p>등재 파급도 게이트가 그대로 잡아 줬다. 문서가 하나 늘자 <strong>지식지도 폴백 3개 언어</strong>(News &amp; Articles 12&rarr;13, km-totals 104·331&rarr;105·335, km-top)와 <strong>OG 카드</strong>(115&rarr;116 docs, 370&rarr;374 concepts)가 동시에 낡았고, 어제 만든 <code>map-fallback-drift</code>·<code>og-card-drift</code>가 즉시 WARN을 냈다. 전부 갱신하고 카드 PNG도 재생성했다 &mdash; <strong>어제의 게이트가 오늘 바로 값을 했다.</strong></p>

<p>관계는 5타입을 훑고 <strong>하나만</strong> 달았다: <code>evidence-for</code> &rarr; <a href="#!hns-review">hns-review</a>(생성 결과 리뷰). Faros 계측과 lights-out 실패는 &ldquo;생성 결과를 사람이 리뷰해야 한다&rdquo;는 그 문서의 <strong>외부 근거</strong>다. 선행지식·구현·사례·대체는 본문에 실재하지 않아 달지 않았다. 개념은 기존 어휘(코딩에이전트·하네스·벤치마크)를 재사용하고 <strong>코드리뷰·강화학습·유지보수성</strong> 3개를 새로 만들었으며, <code>concept-dict-coverage</code>가 요구하는 대로 en·ja 표시명도 함께 넣었다.</p>
