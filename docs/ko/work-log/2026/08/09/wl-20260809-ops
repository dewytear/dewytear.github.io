<h2>2026-08-09 · 운영 — im-not-ai 스킬 설치 + Backlog 정리</h2>

<p>대표님 요청 — GraphDB 실습 편에서 언급된 &lsquo;I&rsquo;m not AI&rsquo; 스킬을 <strong>검색해서 그대로 가져오기</strong>. 변경 유형: <strong>TOOL</strong>.</p>

<p>정체 확인 — 계정 스킬 목록·검색·Anthropic/조직 스킬 제안에는 없었고, 웹 검색으로 커뮤니티 원본을 특정했다: <strong>blader/humanizer</strong>(MIT, 위키피디아 &ldquo;Signs of AI writing&rdquo; 가이드 기반). AI 문체 패턴 <strong>33종</strong> — 의의 부풀리기, 홍보 어투, -ing 겉핥기 분석, 3의 법칙, AI 어휘, 엠대시 금지, 서사적 자기소개("Let's dive in"), 지식 컷오프 면책 등 — 을 잡아 고치고, 사람 글의 신호(구체적 디테일·감정의 미해결·문장 길이 변주)는 보존하라는 오탐 방지 지침까지 갖춘 규칙집이다.</p>

<p class="scn-sub">설치 — 저장소를 익명 읽기로 클론(커밋 <code>523374d</code>)해 <code>SKILL.md</code>를 <strong>원문 그대로</strong> <code>.claude/skills/im-not-ai/</code>에 옮겼다. 변경은 프런트매터의 스킬 이름(<code>humanizer</code> &rarr; <code>im-not-ai</code>)과 출처 메타(<code>metadata.source</code>) 한 줄뿐, MIT 조건대로 <code>LICENSE</code> 원문을 동봉. <strong>프로젝트 스킬이라 이 저장소에서 여는 모든 세션</strong>(아침 브리핑 루틴 포함)에서 <code>/im-not-ai</code>로 호출된다 — 실습 편에서 손으로 흉내 냈던 문체 규칙이 이제 기계가 항상 같은 기준으로 적용하는 도구가 됐다.</p>

<p>덤 — GraphDB 중분류 재편 검토를 <a href="#!wl-backlog">Backlog</a>에 등재(2026-08-09 &lsquo;지금은 유지&rsquo; 결정, 트리거: 문서 5~6편).</p>

<h3>후속 — 콘텐츠 집필 시 im-not-ai 무조건 적용 규칙화</h3>

<p>대표님 지시 &ldquo;콘텐츠 작성 시 자동으로 무조건 사용&rdquo; — 세션 기억이 아니라 <strong>CLAUDE.md 집필 표현 규칙에 영구 조항</strong>으로 박았다. 콘텐츠(본문 문서) 집필·대폭 재작성은 초안 &rarr; im-not-ai 패턴 점검 &rarr; 최종본 순서를 반드시 거치고, 스킬의 오탐 방지 지침(사람 글 신호 보존·사실 불변조)도 함께 따른다. 예시가 영어 기준이라 한국어·일본어 본문에는 상응 패턴으로 옮겨 적용하고, 워크로그는 기록물이라 제외. 규칙이 CLAUDE.md에 있으므로 새로 뜨는 모든 세션(아침 브리핑 루틴 포함)에 자동 적용된다.</p>
